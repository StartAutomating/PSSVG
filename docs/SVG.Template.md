SVG.Template
------------

### Synopsis
Creates a template element.

---

### Description

Creates a template element containing SVG.    
While templates are not _technically_ SVG elements, they're very useful when combined with SVGs.

---

### Syntax
```PowerShell
SVG.Template [<CommonParameters>]
```
